var events = []; $(".events").each(function () { var t = { title: $(".title", this).text().trim(), start: $(".start", this).text().trim() }; events.push(t) }), $("#calendar").fullCalendar({ locale: "au", events: events, dayClick: function (t, e, n, a) { var r = new Date(t), s = moment(r).format("YYYY-MM-DD"); s = encodeURIComponent(s) } });