@model IEnumerable<FIT5032_Assignment.Models.Booking>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>


<div class="row content-padding">
    <div class="col-sm-6">
        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BookingGuestNum)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BookingDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BookingTime)
                </th>
                @*<th>
                    @Html.DisplayNameFor(model => model.Restaraunt.RestAddress)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Customer.CustFirstName)
                </th>*@
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.BookingGuestNum)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BookingDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BookingTime)
                    </td>
                    @*<td>
                        @Html.DisplayFor(modelItem => item.Restaraunt.RestAddress)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Customer.CustFirstName)
                    </td>*@
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.BookingId }) |
                        @Html.ActionLink("Details", "Details", new { id = item.BookingId }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.BookingId })
                    </td>
                </tr>
            }

        </table>
    </div>
    <div class="col-sm-6"></div>
</div>
